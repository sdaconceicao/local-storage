import { useState } from 'react';
import { Meta, Story } from '@storybook/addon-docs/blocks';
import LocalForage from '../components/LocalForage';

<Meta 
  title="LocalForage" 
  parameters={{
    viewMode: 'docs',
    previewTabs: { 'storybook/docs/panel': null, canvas: { hidden: true } },
  }}
/>

## LocalForage

### Comparison

| Pros                         | Cons                                   |
|------------------------------|----------------------------------------|
| Simple API very similar to localstorage | Only supports storing strings | Requires installing a small dependency
| Can store any js types natively | 
| Built on top of IndexDB so access to more storage space |
| Fallbacks to localstorage option if browser does not support IndexDB |


### Example

<Story name="Example">
  <LocalForage/>
</Story>
